I have an xteink 4 and a kobo, and a library that allows me to rent ascm files. I have Calibre, but it is ugly and a bit overkill, but with the ade and obok plugin i can get drm files onto the xteink. I am thinking if it would be possible to just build a small cli or tui that i can feed drm files, and (using the kobo and adobe digital editions) get my epubs on there.
